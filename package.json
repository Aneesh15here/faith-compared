{
  "name": "faith-compared",
  "version": "1.0.0",
  "private": true,
  "description": "Faith, Compared — multi-locale apologetics site (Eleventy)",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve --port=8080",
    "clean": "rm -rf _site",
    "import:legacy": "node scripts/import-legacy.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Aneesh15here/faith-compared.git"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
